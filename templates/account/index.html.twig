{% extends 'account/layout.html.twig' %}

{% set general_settings_active = true %}

{% block account_body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}

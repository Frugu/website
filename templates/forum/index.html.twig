{% extends 'layout/base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col">
            <ul class="list-group">
                {% for row in rows %}
                    {% include 'forum/' ~ row.type ~ '/row.html.twig' with {'row': row} only %}
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
